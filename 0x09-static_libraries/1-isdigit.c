#include "main.h"

/**
 * _isdigit - checks for a digit
 * @dest: input value
 * @c: int to be checked
 * 
 * Return: 1 if c is digit ; otherwise 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
